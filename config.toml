# RPC Configuration
[rpc]
endpoint = "http://localhost:8545"
timeout = 30

# Keystore Configuration
[keystore]
path = "./keystore"

# Logging Configuration
[log]
level = "info"
file = "/var/log/slowmade.log"
encoding = "console"

# UI Configuration
[ui]
lang = "en"
