╔══════════════════════════════════════════════════════════════════╗
║                          *** 钱包详情信息 ***                     ║
╠══════════════════════════════════════════════════════════════════╣
║ 钱包名称: {{.Name}}
║ 钱包ID: {{.ID}}
║ 创建时间: {{.CreationTime.Format "2006-01-02 15:04:05"}}
║ 状态: {{if .IsLocked}}[LOCKED] 已锁定{{else}}[OPEN] 已解锁{{end}}
║ 加密状态: {{if .IsEncrypted}}[ENC] 已加密{{else}}[NO-ENC] 未加密{{end}}
╠══════════════════════════════════════════════════════════════════╣
║                          *** 加密信息 ***                         ║
╠══════════════════════════════════════════════════════════════════╣
{{if .IsEncrypted}}
║ 加密算法: {{.EncryptionMetadata.Algorithm}}
║ 加密版本: {{.EncryptionMetadata.Version}}
║ 加密时间: {{formatTimestamp .EncryptionMetadata.EncryptedAt}}
║ 初始化向量: {{truncate .EncryptionMetadata.IV 16}}...
║ 盐值: {{truncate .EncryptionMetadata.Salt 12}}...
{{else}}
║ 该钱包未启用加密保护
{{end}}
╠══════════════════════════════════════════════════════════════════╣
║                          *** 币种资产 ***                         ║
╠══════════════════════════════════════════════════════════════════╣
{{if .Coins}}
{{range $index, $coin := .Coins}}
║ {{add $index 1}}. {{$coin.Symbol}} ({{$coin.Chain}})
║    地址: {{$coin.Address}}
║    路径: {{$coin.Path}}
║    索引: {{$coin.Index}}
{{if not (last $index $.Coins)}}║{{end}}
{{end}}
{{else}}
║ 暂无币种配置
║ 使用 'add-coin' 命令添加币种支持
{{end}}
╚══════════════════════════════════════════════════════════════════╣
